module Otel.Client (
  startOtelClient,
  OtelClientParameters (..),
  OtelClient,
  defautOtelClientParameters,
) where

import Otel.Internal.Client (OtelClient, OtelClientParameters (..), defautOtelClientParameters, startOtelClient)
